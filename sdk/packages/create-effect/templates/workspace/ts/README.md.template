# __WORKSPACE_NAME__

Hypercolor effect workspace.

## Commands

- `bun run build` builds every effect into `dist/`
- `bun run validate` checks built artifacts
- `bun run ship` installs locally

## Layout

- `effects/` holds your source effects
- `dist/` is generated build output
